<Window x:Class="ComputerPassport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Паспорт Компьютера - Сбор системной информации"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Icon="/Resources/logo.png"
        MinHeight="600" MinWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель навигации -->
        <Border Grid.Row="0" 
                Background="{DynamicResource PrimaryHueMidBrush}"
                Margin="0,0,0,20" 
                Padding="24,16" 
                CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnBack" 
                        Content="← НАЗАД" 
                        Style="{StaticResource SecondaryButton}"
                        Click="BtnBack_Click" 
                        Visibility="Collapsed"
                        Foreground="White"
                        BorderBrush="White"
                        Grid.Column="0"/>

                <TextBlock x:Name="txtNavigationTitle" 
                           Text="Паспорт компьютера" 
                           Foreground="White" 
                           FontWeight="Bold" 
                           FontSize="20" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Grid.Column="1"/>

                <Button x:Name="btnAbout" 
                        Content="?" 
                        Style="{StaticResource SecondaryButton}"
                        Click="BtnAbout_Click"
                        Foreground="White"
                        BorderBrush="White"
                        Width="40"
                        Height="40"
                        FontSize="16"
                        Grid.Column="2"
                        Margin="10,0,0,0"
                        ToolTip="О программе"/>
            </Grid>
        </Border>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель - Управление -->
            <Border x:Name="mainContent" 
                    Grid.Column="0"
                    Style="{StaticResource CardStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <!-- Выбор разделов для сбора -->
                        <TextBlock Text="📋 Выберите разделы для сбора:" 
                                   Style="{StaticResource SectionHeader}"/>

                        <StackPanel Margin="0,0,0,8">
                            <CheckBox x:Name="cbHardware" 
                                      Content="💻 Аппаратная конфигурация" 
                                      Style="{StaticResource MaterialDesignCheckBox}"
                                      Margin="0,0,0,6"
                                      Checked="CheckBox_CheckedChanged" 
                                      Unchecked="CheckBox_CheckedChanged"/>
                            <CheckBox x:Name="cbSoftware" 
                                      Content="🖥️ Программное обеспечение" 
                                      Style="{StaticResource MaterialDesignCheckBox}"
                                      Margin="0,0,0,6"
                                      Checked="CheckBox_CheckedChanged" 
                                      Unchecked="CheckBox_CheckedChanged"/>
                            <CheckBox x:Name="cbDrivers" 
                                      Content="⚙️ Драйверы устройств" 
                                      Style="{StaticResource MaterialDesignCheckBox}"
                                      Margin="0,0,0,6"
                                      Checked="CheckBox_CheckedChanged" 
                                      Unchecked="CheckBox_CheckedChanged"/>
                            <CheckBox x:Name="cbPeripherals" 
                                      Content="🖱️ Периферийные устройства" 
                                      Style="{StaticResource MaterialDesignCheckBox}"
                                      Margin="0,0,0,16"
                                      Checked="CheckBox_CheckedChanged" 
                                      Unchecked="CheckBox_CheckedChanged"/>
                        </StackPanel>

                        <!-- Действия -->
                        <Button x:Name="btnCollect" 
                                Content="🔍 Собрать информацию" 
                                Style="{StaticResource PrimaryButton}"
                                Click="BtnCollect_Click"
                                Height="40"/>

                        <Button x:Name="btnBrowseFolder" 
                                Content="📁 Выбрать папку для сохранения" 
                                Style="{StaticResource SecondaryButton}"
                                Click="BtnBrowseFolder_Click"
                                Height="40"/>

                        <!-- Информация о папке сохранения -->
                        <TextBlock Text="📂 Папка для сохранения:" 
                                   FontWeight="SemiBold"
                                   Margin="0,16,0,8"/>

                        <TextBox x:Name="txtSaveFolder" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Укажите путь для сохранения файлов..."
                                 Margin="0,0,0,8"/>

                        <TextBlock x:Name="txtFolderStatus" 
                                   Text="📍 Статус папки: не проверен" 
                                   FontSize="12"
                                   Foreground="{DynamicResource MaterialDesignBody}"
                                   Margin="0,0,0,20"/>

                        <!-- Выбор формата экспорта -->
                        <TextBlock Text="📤 Экспорт данных:" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>

                        <ComboBox x:Name="cmbExportFormat"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  materialDesign:HintAssist.Hint="Выберите формат экспорта"
                                  Margin="0,0,0,8"
                                  FontSize="14"
                                  Height="51">
                            <ComboBoxItem Content="🌐 HTML документ" Tag="HTML"/>
                            <ComboBoxItem Content="📄 PDF документ" Tag="PDF"/>
                            <ComboBoxItem Content="💾 JSON данные" Tag="JSON"/>
                        </ComboBox>

                        <Button x:Name="btnExport" 
                                Content="🚀 Экспортировать" 
                                Style="{StaticResource PrimaryButton}"
                                Click="BtnExport_Click"
                                Margin="0,0,0,16"
                                Height="40"/>

                        <!-- Кнопка сравнения -->
                        <Button x:Name="btnCompare" 
                                Content="📊 Сравнить с сохранённым" 
                                Style="{StaticResource PrimaryButton}"
                                Click="BtnCompare_Click"
                                Margin="0,0,0,16"
                                Height="40"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Правая панель - Вывод информации -->
            <Border Grid.Column="2" 
                    Style="{StaticResource CardStyle}"
                    Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="📊 Собранная информация:" 
                               Style="{StaticResource SectionHeader}"
                               Grid.Row="0"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled"
                                  Grid.Row="1">
                        <TextBlock x:Name="txtOutput" 
                                   FontFamily="Consolas" 
                                   FontSize="12" 
                                   TextWrapping="Wrap"
                                   LineHeight="18"
                                   Foreground="{DynamicResource MaterialDesignBody}">
                            Соберите информацию для начала работы...
                        </TextBlock>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Контент сравнения (скрыт) -->
            <Border x:Name="comparisonContent" 
                    Grid.Column="0" Grid.ColumnSpan="3"
                    Background="{DynamicResource MaterialDesignPaper}" 
                    Visibility="Collapsed">
                <Frame x:Name="comparisonFrame" 
                       NavigationUIVisibility="Hidden"/>
            </Border>

            <!-- Контент "О программе" (скрыт) -->
            <Border x:Name="aboutContent" 
                    Grid.Column="0" Grid.ColumnSpan="3"
                    Background="{DynamicResource MaterialDesignPaper}" 
                    Visibility="Collapsed">
                <Frame x:Name="aboutFrame" 
                       NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
    </Grid>
</Window>